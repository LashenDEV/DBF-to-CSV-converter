#!/bin/bash
set -e

# Make sure the executable is... executable
chmod +x /usr/bin/dbf-converter

# Create a virtual environment and install dbfread
python3 -m venv /usr/lib/dbf-converter/venv
/usr/lib/dbf-converter/venv/bin/pip install dbfread

echo "DBF to CSV Converter installed successfully!"
